# Placeholder. The real consumer CMakeLists.txt is generated by
# check_source_cache_reuse.cpp for each scenario (it injects the
# FetchContent_Declare() origin under test and opts into CMP0168 NEW).
# This file only exists so the template directory is valid before the
# test rewrites it.
cmake_minimum_required(VERSION 3.27.6)
project(CheckSourceCacheReuse LANGUAGES CXX)
