# Placeholder. The real consumer CMakeLists.txt is generated by
# hfc_source_migration_test.cpp for each migration step (it injects the
# FetchContent_Declare() origin under test). This file only exists so the
# template directory is valid before the test rewrites it.
cmake_minimum_required(VERSION 3.27.6)
project(HfcSourceMigrationTest LANGUAGES CXX)
