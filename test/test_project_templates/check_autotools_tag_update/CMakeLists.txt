# Placeholder. check_autotools_tag_update.cpp generates the real consumer
# CMakeLists.txt for each tag (v1 then v2), pointing at the real autotools sample.
cmake_minimum_required(VERSION 3.27.6)
project(AutotoolsTagUpdate LANGUAGES CXX)
